# Amethyst CLI

A CLI for Amethyst Launcher that connects to the GUI app through its MCP server.

Based on [SJMCL CLI](https://github.com/UNIkeEN/SJMCL/tree/main/cli).

Build: 

```bash
cargo build --release --manifest-path cli/Cargo.toml
```
